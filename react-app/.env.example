# Salesforce Connected App — OAuth 2.0 Client Credentials flow
# Set these in Railway dashboard (never commit the real values)
SF_LOGIN_URL=https://login.salesforce.com
SF_CLIENT_ID=<Connected App Consumer Key>
SF_CLIENT_SECRET=<Connected App Consumer Secret>

# Optional: Agentforce Headless API (for chat route)
SF_AGENT_API_URL=https://your-instance.salesforce.com/einstein/ai-agent/v1
SF_AGENT_ID=your-agent-id
